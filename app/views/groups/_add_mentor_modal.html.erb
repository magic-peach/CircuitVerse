<div id="add-mentor-panel" class="cv-inline-panel" role="region" aria-label="<%= t('add_mentors') %>">
  <div class="cv-inline-panel-inner">
    <div class="cv-panel-header">
      <strong style="color:#0F6E56; font-size:0.9rem;"><%= t("add_mentors") %></strong>
      <button type="button" class="cv-panel-close" onclick="closeCvPanel('add-mentor-panel')" aria-label="Close">
        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg>
      </button>
    </div>

    <p style="font-size:0.85rem; color:#555; margin-bottom:10px;"><%= t("groups.add_member_description") %></p>
    <%= t("groups.add_mentor_description_html") %>

    <%= form_with(url: group_members_path, local: true) do |form| %>
      <input type="hidden" name="group_member[group_id]" value="<%= group_member.group_id %>">
      <input type="hidden" name="group_member[mentor]" value="true">

      <div class="cv-panel-input-row" style="margin-top:10px;">
        <div style="flex:1; min-width:0;">
          <textarea name="group_member[emails]" 
                    placeholder="Enter email(s) - separate with commas or new lines"
                    class="form-control"
                    style="width:100%; padding:8px 12px; border:1px solid #D1EDE4; border-radius:6px; font-size:0.9rem;"
                    rows="3"></textarea>
          <small class="form-text text-muted" style="color:#888; font-size:0.75rem;">
            Enter email addresses separated by commas or on separate lines
          </small>
        </div>
        <button type="submit" class="btn primary-button"
                style="height:38px; white-space:nowrap; flex-shrink:0; align-self:flex-start;">
          <%= t("add_mentors", default: "Add Mentors") %>
        </button>
      </div>
    <% end %>
  </div>
</div>
